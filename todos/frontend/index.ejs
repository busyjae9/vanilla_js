<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>TODOS</title>
    <meta name="theme-color" content="#6b7280">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-status-bar-style" content="#6b7280">
    <meta name="theme-color" content="black">
    <link
            rel="manifest"
            href='<%= process.env.url; %>:<%= process.env.ENV === 'dev' ? process.env.port_test : process.env.port; %>/dist/manifest.json'
    />
    <link
            type='text/css'
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />
    <link
            type='text/css'
            rel='stylesheet'
            href='<%= process.env.url; %>:<%= process.env.ENV === 'dev' ? process.env.port_test : process.env.port; %>/dist/app.bundle.css'>
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
</head>
<body>
<script type="module"
        src='<%= process.env.url; %>:<%= process.env.ENV === 'dev' ? process.env.port_test : process.env.port; %>/dist/app.bundle.js'></script>
<% if(locals.whoami) { %>
    <div class="whoami">
        <div class="whoami__container">
            <% if(locals.user && locals.whoami.id !== locals.user?.id) { %>
                <div class="whoami__info">
                    <ion-icon class="whoami__info__icon" name="person"></ion-icon>
                    <div class="whoami__info__name"><%= locals.user?.name %> 님</div>
                </div>
            <% } else { %>
                <div class="whoami__info">
                    <ion-icon class="whoami__info__icon" name="person"></ion-icon>
                    <div class="whoami__info__name"><%= locals.whoami.name %></div>
                </div>
            <% } %>
            <% if(locals.user && locals.whoami.id !== locals.user?.id) { %>
                <div class="whoami__buttons">
                    <button class="whoami__buttons__search">검색<span class='mini__text'>(cmd+shift+.)</span></button>
                    <button class="whoami__buttons__my_page">내 페이지</button>
                </div>
            <% } else { %>
                <% if(locals.url.includes("archive")) { %>
                    <div class="whoami__buttons">
                        <button class="whoami__buttons__del_all">전체 삭제</button>
                        <button class="whoami__buttons__return">돌아가기</button>
                    </div>
                <% } else { %>
                    <div class="whoami__buttons">
                        <button class="whoami__buttons__search">검색<span class='mini__text'>(cmd+shift+.)</span></button>
                        <button class="whoami__buttons__archive">보관함</button>
                        <button class="whoami__buttons__logout">로그아웃</button>
                    </div>
                <% } %>
            <% } %>

        </div>
    </div>
<% } %>
<% if(locals.body) { %>
    <%- locals?.body; %>
<% } %>
</body>
</html>
